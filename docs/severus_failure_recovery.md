# Severus Failure Recovery

Severus plans classify failures into configuration, version, preflight, command, execution, timeout, output, VCF, BND, index, QC, and tumor-only policy categories.

Rerun manifests are selected from explicit status and failure-category filters. The harness does not automatically delete outputs or resubmit jobs.
