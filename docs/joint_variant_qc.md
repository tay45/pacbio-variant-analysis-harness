# Joint Variant QC

Phase 2C technical QC reports total samples, variant counts, SNV/indel counts,
PASS/FILTER counts, Ti/Tv where calculable, genotype missingness, singleton and
doubleton counts, per-contig counts, and per-sample genotype counts.

QC does not implement biological interpretation or pathogenicity assessment.

