# DeepSomatic Provenance

Provenance records config, manifest, pair, input, index, reference, model,
container/executable, command, region, PoN, runtime, exit code, output checksum,
validation, QC, package version, and supersession metadata. Credentials and
tokens are not recorded.
