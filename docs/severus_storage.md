# Severus Storage

Storage estimates are planning approximations based on known input sizes and whether intermediates are preserved. Input data is never deleted by Severus storage helpers.

Cleanup policy must remain opt-in, documented, and limited to harness-created temporary files.
