# DeepSomatic Storage

Storage and scratch values are planning approximations for tumor/normal inputs,
temporary data, intermediate results, VCF/gVCF outputs, indexes, logs, QC,
reports, and container cache. Inputs are never deleted.
