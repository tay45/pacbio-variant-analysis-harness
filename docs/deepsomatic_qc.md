# DeepSomatic QC

Technical QC summarizes FILTER counts, SNV/indel counts, multiallelic records,
Ti/Tv, VAF, DP, AD, GQ, contig counts, malformed records, validation status,
caller version, and model type. Missing FORMAT values remain missing.
