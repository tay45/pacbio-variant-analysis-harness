# Severus Provenance

Each planned command records argv, sanitized argv, command signature, input paths, output paths, caller configuration, version policy, execution mode, and attempt ID.

Future real execution should add hostname, scheduler IDs, resource usage, stdout/stderr paths, and validation outcomes per attempt.
