# DeepSomatic Outputs

Per-pair outputs live under `small_variants/deepsomatic/` with command, logs,
intermediate, temporary, output, validation, QC, provenance, and status
subdirectories. Primary VCF outputs retain all FILTER categories.
