# Site Profiles

Create external site profiles outside the repository or under a private ignored
location. Do not commit real accounts, queues, storage paths, credentials, or
private container locations.

Use the neutral examples in `configs/` as templates.

Slurm profile values are validated before script generation. Generated scripts
are reviewable artifacts and are not automatically submitted in Phase 2A.1.
