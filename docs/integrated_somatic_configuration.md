# Integrated Somatic Configuration

Integrated reporting is disabled by default under `somatic.integrated.enabled`.
The project policy controls whether small variants and structural variants are
`required`, `optional`, or `disabled`; whether partial success is allowed; and
whether warning outputs can contribute.

Relationship windows are configurable and are technical context only. There is
no universal biological window, and missing data remains explicit.
