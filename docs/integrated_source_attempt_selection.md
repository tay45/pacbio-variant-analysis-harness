# Integrated Source Attempt Selection

Integrated reports reference DeepSomatic and Severus source attempts by pair ID,
attempt ID, path, validation status, QC status, checksum fields, sample identity,
and reference identity. Superseded attempts are excluded by default.

The default selection policy is deterministic and uses validated complete
attempts. Explicit attempt IDs may be supplied when operators need pinned
integration.
