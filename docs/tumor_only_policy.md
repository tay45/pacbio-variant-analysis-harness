# Tumor-Only Policy

Tumor-only analysis is disabled by default. It requires explicit project
permission, `analysis_mode=tumor_only`, and acknowledgment when configured.

Phase 2D records tumor-only limitations, including elevated germline
contamination risk, reduced somatic specificity, and dependence on future
caller-specific filtering. It does not implement population filtering or claim
tumor-only equivalence to matched-normal analysis.
